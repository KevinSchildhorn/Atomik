package com.kevinschildhorn.android.ui.screens.compose

/*
@Preview
@Composable
fun PrimaryButton(onClick:() -> Unit, molecule: TextButtonMolecule = FluentAtoms.primaryButton) {
    Button(
        colors = molecule.buttonColors(),
        modifier = Modifier.clip(molecule.shape),
        onClick = onClick
    ) {
        PrimaryButtonText(
            text = "Click Here!",
            atom = molecule.textViewAtom
        )
    }
}

@Composable
fun PrimaryButtonText(text:String, atom: TextViewAtom) {
    Text(
        text = text,
        color = atom.textColor.platformColor,
        fontFamily = atom.fontFamily?.fontFamily,
        fontStyle = atom.typography.
    )
}*/